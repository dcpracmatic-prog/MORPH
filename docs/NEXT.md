# Next step

The project does not need another round of solver-vs-solver comparisons before publication. The useful next step is engineering and packaging: keep the benchmark fixed, improve memory efficiency of the residual-degree core, and publish reproducible runs with raw CSVs and environment metadata.
